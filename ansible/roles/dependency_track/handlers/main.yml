---
# roles/dependency_track/handlers/main.yml

- name: Restart dtrack
  become: true  # root: manages the dtrack systemd unit
  ansible.builtin.systemd:
    name: dtrack
    state: restarted
    daemon_reload: true

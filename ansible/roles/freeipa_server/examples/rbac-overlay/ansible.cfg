[defaults]
# Make the freeipa_server role discoverable and load its filter_plugins (freeipa_rbac /
# freeipa_idam) so the overlay compiles.
roles_path = ../../../
filter_plugins = ../../filter_plugins
jinja2_native = True
inventory = inventory.yml
host_key_checking = False

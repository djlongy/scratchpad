{# {{ ansible_managed }} — pre-auth console/SSH banner.
   Explicit \n: Ansible's Jinja trim_blocks would otherwise glue the legal line
   onto the host line when baseline_banner_message is empty. #}
{{ baseline_banner_env_label }} environment — {{ baseline_banner_fqdn }}{% if baseline_banner_ip | length > 0 %} ({{ baseline_banner_ip }}){% endif %}{{ "\n" }}{% if baseline_banner_message | length > 0 %}{{ baseline_banner_message }}{{ "\n" }}{% endif %}Authorised access only. Activity may be monitored and logged.

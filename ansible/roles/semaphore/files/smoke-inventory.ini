# Smoke-test inventory — single localhost entry pinned to the local
# connection plugin. Used by Semaphore's smoke template to prove the
# orchestrator can run an ansible play end-to-end without touching
# remote hosts.

[local]
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3

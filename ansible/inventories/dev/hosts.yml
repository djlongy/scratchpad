# Simulated 'dev' environment for the artifactory role. Points at the SAME
# Artifactory instance as inventories/prod, so dev->prod promotion can be
# exercised end-to-end on a single trial. API calls run from the controller.
all:
  hosts:
    localhost:
      ansible_connection: local

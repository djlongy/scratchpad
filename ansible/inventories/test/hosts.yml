# Simulated 'test' environment for the artifactory role. API calls run from the
# controller (the artifactory.yml play targets localhost). The real URL and the
# per-environment system-config overrides live in the gitignored
# group_vars/all/local.yml.
all:
  hosts:
    localhost:
      ansible_connection: local

---
# ops_splunk — control-node inventory for API-only export/apply.
# No remote hosts: management REST runs on localhost against splunk_config_api_url.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python | default('/usr/bin/python3') }}"

# Simulated 'prod' environment for the artifactory role. Points at the SAME
# Artifactory instance as inventories/dev (a single trial), so a dev->prod
# promotion can be exercised end-to-end. API calls run from the controller.
all:
  hosts:
    localhost:
      ansible_connection: local
